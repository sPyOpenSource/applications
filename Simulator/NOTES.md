# Teaching Notes

## User preferences

- Ground every lesson in real files in `src/` — no toy examples (MISSION.md constraint).
- User knows Java well; no language basics.
- Lessons must be scannable in 10–15 minutes; short, one tangible win each.
- Lesson style follows `0002-special-function-registers.html` (lesson.css, exercise block, details-reveal, nav + footer with primary source and "ask your instructor" prompt).
- Quizzes: options should be equal-length so formatting gives no clue (quiz.js component in assets/).
- Primary source should be the most high-quality, high-trust resource found on the topic (see RESOURCES.md).
